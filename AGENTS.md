# ZeroTrust 프로젝트 개발 원칙

이 문서는 Codex가 이 저장소에서 작업할 때 따르는 개발 지침이다. 사용자 요청을 실제 코드·검증·기록까지 완료하되, 합의하지 않은 정책 변경과 작업 범위 확대를 하지 않는다.

## 1. 프로젝트 범위

- 목적: 수사정보의 민감도와 사용자·환경·행동·업무 관계를 평가하는 동적 접근제어 시스템.
- 핵심: 4축 위험 평가, L1~L5 접근 결정, 사건 담당 관계, MFA, 관리자 승인, 세션 검증, Break-Glass, 감사 기록.
- 현재 기본 구성: Python/Tornado, PostgreSQL, React 기반 화면. 실제 파일과 실행 경로는 작업 시 확인한다.
- AI·LLM·머신러닝 접목은 현재 범위에서 제외한다. 관련 의존성이나 빈 모듈도 미리 추가하지 않는다.
- DB 교체, 프레임워크 교체, 전면 재작성, 대규모 디렉터리 개편은 요청 없이 진행하지 않는다.
- 시연용 환경 신호와 실제 검증된 보안 신호를 구분한다. 시연 성공을 운영 보안성 검증으로 설명하지 않는다.

## 2. 작업 시작 순서

1. `git status --short --branch`로 현재 브랜치와 사용자 변경 사항을 확인한다.
2. 루트와 작업 대상 경로의 `AGENTS.md`, README, 관련 정책 문서·테스트를 읽는다.
3. 관련 코드의 실제 호출 경로를 확인한다. 함수나 파일이 있다는 이유만으로 기능이 연결됐다고 판단하지 않는다.
4. 요청을 작업 목표, 변경 범위, 완료 조건으로 짧게 정리한 뒤 구현을 시작한다.
5. 기존 기능·테스트·문서를 재사용하고 필요한 부분만 수정한다.

- 일반적인 구현 선택은 스스로 판단하고 진행한다. 이미 요청된 작업에 대해 반복 확인하지 않는다.
- 권한 범위, 점수 정책, 데이터 보존 등 결과를 바꾸는 요구가 불명확할 때만 구체적인 선택지를 묻는다. 확정된 부분의 작업은 계속한다.
- 사용자 변경을 덮어쓰거나 요청과 무관한 파일을 정리하지 않는다. 독립 작업이 필요하면 별도 브랜치나 worktree를 사용한다.
- 보고서·주석·코드가 다르면 현재 동작과 합의된 의도를 나눠 기록한다. 현재 구현을 자동으로 올바른 정책으로 확정하지 않는다.

## 3. Git과 버전 관리

- 브랜치 운영은 `CONTRIBUTING.md`를 따른다. `develop`에서 `feat/짧은-주제`를 분기하고, 검증 후 `develop`에 통합한 다음 배포 기준을 `main`에 반영한다. 수정·문서 작업도 같은 경로를 사용한다.
- 현재 작업에 적합한 브랜치가 있으면 재사용한다. 작업 트리가 깨끗할 때만 브랜치를 전환하고, 무조건 최신 원격으로 reset하지 않는다.
- 한 커밋·PR은 하나의 목적을 가진다. 해당 목적의 코드·테스트·문서 변경은 함께 묶는다.
- 커밋 형식: `유형(영역): 변경 내용`. 예: `fix(auth): 승인 상태 검증 통일`.
- 유형은 `feat`, `fix`, `refactor`, `test`, `docs`, `build`, `ci`, `chore`를 사용한다.
- 커밋·push·원격 Issue/PR 작성·merge·태그·배포는 사용자 요청이나 기존 승인이 있는 범위에서 수행한다. 승인이 없으면 로컬 수정과 검증, 커밋 메시지·PR 초안까지 준비한다.
- 커밋할 때 파일을 명시적으로 선택한다. 사용자의 기존 변경과 비밀값을 함께 포함하지 않는다.
- 공유 브랜치의 강제 push, 이력 재작성, `reset --hard`, 무차별 `git clean`은 금지한다. 필요하면 손실 범위를 설명하고 명시적 승인을 받는다.

### 릴리스 기준

- 버전은 `vMAJOR.MINOR.PATCH` 태그로 표시한다. 기존 태그와 버전 파일을 먼저 확인하고 임의로 새 체계를 만들지 않는다.
- 개발 단계 `0.x`: 기능 추가·의도적인 접근 정책 변경은 MINOR, 기존 명세에 맞춘 버그 수정은 PATCH를 올린다. 사용자에게 영향을 주는 변경은 반드시 기록한다.
- `1.0.0`은 합의된 필수 기능과 검증을 완료한 기준 버전으로 사용자가 확정한다. 이후 호환성을 깨는 변경은 MAJOR를 올린다.
- 매 커밋마다 버전을 올리지 않는다. 검증된 시연·배포 기준을 확정할 때 태그를 만든다.
- 태그는 실제 검증한 커밋에 붙이고 재사용하거나 이동하지 않는다. 릴리스에 필요한 DB 마이그레이션과 복구 조건도 기록한다.

## 4. 작업 계획과 수정 내역

문서의 역할을 다음처럼 구분한다. 기존에 같은 역할의 파일이 있으면 먼저 통합하고 중복 문서를 늘리지 않는다.

| 위치 | 기록할 내용 |
| --- | --- |
| `README.md` | 프로젝트 목적, 구성, 안전한 실행 방법, 현재 한계 |
| `ROADMAP.md` | 큰 단계, 우선순위, 완료 조건, 진행 상태 |
| `docs/POLICY.md` | 합의된 접근·점수·승인·예외 정책과 미확정 사항 |
| `CHANGELOG.md` | 버전별 주요 기능·수정·정책 변경·호환성 영향 |
| GitHub Issue | 개별 작업의 문제, 범위, 완료 조건 |
| PR | 변경 이유, 적용 내용, 영향, 검증 결과, 관련 Issue |

- Issue를 사용할 수 없거나 원격 작성 승인이 없으면 해당 작업의 목표·완료 조건을 `ROADMAP.md`의 작업 항목에 기록한다. 작은 문구 수정까지 Issue를 요구하지 않는다.
- 구현이 필요한 작업은 완료 조건을 먼저 정한다. 예: “승인 상태별 서버 판단과 화면 버튼 상태가 일치한다.”
- 기능·버그·정책·운영 절차에 의미 있는 변화가 있으면 `CHANGELOG.md`의 `Unreleased`에 기록한다. 사소한 문구·서식 수정은 Git 기록으로 충분하다.
- 릴리스 시 `Unreleased` 항목을 확정한 버전과 실제 날짜 아래로 이동한다. 존재하지 않는 과거 버전이나 작업 이력을 만들지 않는다.
- 변경 기록은 “무엇을, 왜, 어떤 영향으로 바꿨는지”를 적는다. “수정 완료” 같은 문장만 남기지 않는다.
- ROADMAP의 완료 표시는 완료 조건을 충족했을 때만 한다. 구현했지만 검증하지 못했으면 `검증 대기`로 표시한다.

변경 기록 예시:

```markdown
## Unreleased

### Fixed
- [영역] 변경 내용. 변경 이유와 사용자에게 달라지는 동작.

### Policy
- 이전 조건 → 변경 조건. 결정 근거, 영향받는 시나리오, 관련 Issue/PR.
```

## 5. 접근 정책 변경 원칙

- 점수 가중치, 결정 경계, 재인증 TTL, 승인 범위, 즉시 차단과 긴급 접근 조건을 UI 편의나 테스트 통과만을 위해 바꾸지 않는다.
- 정책 변경은 사용자 요청 또는 합의된 작업 범위에 근거해야 한다. 이전 조건·새 조건·이유·예상 결과를 기록한다.
- 정책을 변경하면 구현, 관련 테스트, `docs/POLICY.md`, 사용자 설명을 같은 작업에서 갱신한다.
- 서버가 권한을 최종 판단한다. 버튼 비활성화·CSS 블러·클라이언트 플래그를 권한 통제로 취급하지 않는다.
- 목록·상세·상태 조회·다운로드에 공통 정책을 적용한다. 응답에 포함할 필드도 권한에 따라 서버에서 결정한다.
- 인증·승인 전 미리보기를 허용할지 명시한다. 열람 금지 정책이면 본문을 응답에 포함하지 않는다.
- OTP 필수 여부, 승인 가능한 상태, 승인자 권한, 자기 승인 금지, 만료·취소를 서버에서도 검증한다.
- 사건 담당 등록, 유효기간이 있는 자료 접근 승인, Break-Glass를 서로 다른 권한으로 유지한다. 요청 없이 영구 권한으로 전환하지 않는다.
- 위험 점수, 점수에 따른 기본 레벨, 예외 적용 후 최종 레벨을 구분한다. 화면 표시를 맞추려고 원래 점수를 덮어쓰지 않는다.
- 사용자 응답에는 합의된 최소 정보만 포함한다. 내부 판단 근거는 권한이 있는 감사 경로에서 제공한다.
- 폴링·자동 갱신이 사용자 행동 횟수나 위험도를 부당하게 올리지 않도록 한다.
- 복사·인쇄 제한과 위치·단말 신뢰는 실제 수집·검증·집행 경로까지 확인한다. 권한 값이나 함수만 있으면 `부분 구현`으로 기록한다.
- 단순화·비활성화한 기능은 문서와 화면에서도 현재 상태를 정확히 설명한다.

## 6. 데이터와 구현 원칙

- PostgreSQL을 유지한다. SQL 값은 파라미터 바인딩으로 전달한다.
- 적용된 마이그레이션은 수정하지 않는다. 실제 마지막 번호를 확인해 다음 번호의 파일을 추가한다.
- 운영 적용은 새 마이그레이션으로 전진한다. 기존 DOWN 파일의 존재와 실행 가능성은 별도로 확인하고, 되돌리기가 가능하다고 추정하지 않는다.
- 마이그레이션의 데이터 영향, 실행 순서, 실패 시 복구 방법을 정리하고 격리된 테스트 DB에서 검증한다.
- 감사 이력은 보존한다. 애플리케이션 기능에서 기존 감사·민감 로그를 임의 수정·삭제하지 않는다. 현재 DB가 실제로 보장하는 범위는 확인 후 설명한다.
- 실행·테스트 스크립트를 먼저 읽는다. `wipe`, `reseed`, 테이블 삭제 등이 포함되면 대상 DB가 폐기 가능한 테스트 DB인지 확인한다. 운영 데이터 초기화를 일반 실행 절차로 수행하지 않는다.
- 비밀번호, OTP, 토큰, 비밀키, 실제 개인정보, DB 덤프를 커밋·로그·응답에 남기지 않는다. `.env.example`에는 자리표시자만 넣는다.
- 암호화되지 않은 데이터를 암호화된 것으로 설명하지 않는다. 테스트 통과를 위해 인증·인가·감사 보호 장치를 끄지 않는다.
- 실패를 넓은 예외 처리로 숨기거나 빈 성공 응답으로 바꾸지 않는다. 권한 판단에 필요한 상태를 확인할 수 없으면 허용으로 처리하지 않는다.
- 기존 시드 계정의 역할·신뢰도·담당 관계는 시연 입력이다. 변경이 필요하면 관련 시나리오와 기대 결과를 함께 갱신한다.
- 새 의존성은 기존 기능으로 해결할 수 없을 때만 추가하고 이유를 기록한다. 무관한 리팩터링·전체 포맷 변경은 섞지 않는다.

## 7. 검증과 완료 조건

- 작업 전 관련 테스트의 의도와 실행 방법을 확인한다. 과거 문서의 통과 개수를 현재 결과로 재사용하지 않는다.
- 기능 추가·버그 수정은 핵심 동작이나 재발 방지를 확인하는 테스트를 추가·수정한다. 문구·서식 변경에는 불필요한 테스트를 만들지 않는다.
- 정책 변경은 정상 허용, 제한·거부, 점수 경계, 인증·승인 전후를 확인한다. 관련된 경우 만료·취소·자기 승인 방지도 검증한다.
- 응답 제한을 바꿨으면 화면뿐 아니라 API 응답 필드도 확인한다. UI 변경은 가능하면 실제 화면 흐름을 확인한다.
- 먼저 관련 테스트를 실행한다. 공통 인증·정책·세션·DB 변경은 영향받는 시나리오까지 확대하고, main 반영·릴리스 전에는 프로젝트의 전체 회귀 검증을 수행한다.
- DB 테스트는 대상 연결이 격리된 테스트 DB임을 확인한 뒤 실행한다. 환경이 없다고 SQLite 대체나 보안 로직 제거로 통과시키지 않는다.
- 테스트를 통과시키려고 기대값을 무조건 현재 코드에 맞추지 않는다. 합의된 정책과 테스트 의도를 먼저 확인한다.
- 실행할 수 없으면 미실행 항목·원인·남은 확인을 기록한다. 구문 검사, 단위 테스트, DB 통합 테스트, 화면 검증 결과를 구분한다.
- 마지막에 `git diff --check`, diff와 변경 파일 목록을 확인해 비밀값·임시 파일·무관한 변경이 없는지 점검한다.

완료 체크:

- [ ] 요청 범위와 완료 조건을 충족했다.
- [ ] 기존 사용자 변경과 관련 없는 기능을 보존했다.
- [ ] 필요한 정책·사용법·변경 기록을 갱신했다.
- [ ] 관련 검증을 수행했고 미검증 부분을 명시했다.
- [ ] 커밋·원격 반영 여부를 정확히 보고했다.

## 8. 완료 보고 형식

한국어로 다음 항목을 짧게 보고한다. 사용자가 판단할 수 있도록 파일 경로와 필요한 근거를 붙인다.

1. 변경: 무엇을 왜 바꿨는지, 주요 변경 파일.
2. 동작 영향: 사용자 흐름·정책·DB·기존 기능에 달라지는 점.
3. 검증: 실제 실행 명령과 결과, 미실행 항목과 이유.
4. Git 상태: 작업 브랜치, 커밋·push 여부. 수행하지 않았으면 명시.
5. 남은 작업: 완료를 막는 항목 또는 별도 후속 작업. 없으면 생략.

확인하지 않은 동작을 “완료”, 실행하지 않은 테스트를 “통과”로 표현하지 않는다.

## 9. 이 지침을 처음 적용할 때

- 이 파일은 저장소 루트의 `AGENTS.md`로 배치하는 것을 기준으로 한다.
- 기존 `source/AGENTS.md` 등 하위 지침을 함께 확인한다. 지침 정리 작업에서는 공통 규칙을 루트에 모으고 하위에는 해당 경로의 추가 규칙만 남긴다. 보존할 제약을 검토하지 않고 파일을 삭제하지 않는다.
- 문서 정리 요청은 접근 정책 변경 승인이 아니다. 충돌하는 정책 설명은 `미확정`으로 기록하고 실제 기능을 임의로 바꾸지 않는다.
- 필요한 문서가 없으면 현재 확인된 사실과 합의된 의도만으로 최소한의 초안을 작성한다. 코드 상태, 미검증 상태, 결정이 필요한 항목을 구분한다.
- 기존 Issue·태그·변경 이력이 있으면 이어서 사용한다. 새 버전 번호, 테스트 통과 기록, 정책 변경 승인을 지어내지 않는다.

## 10. 토큰과 작업 비용 절약

불필요한 입력·출력·반복 작업을 최소화한다. 토큰 절약을 이유로 필수 지침 확인, 요청 범위, 보안 검증, 정확한 완료 보고를 생략하지 않는다. 이 지침은 사용량의 정확한 수치나 절감률을 보장하지 않는다.

### 필요한 범위만 읽기

- 적용되는 지침을 먼저 읽고, `rg --files`와 `rg -n`으로 관련 경로·심볼을 찾은 뒤 필요한 함수와 주변 문맥부터 읽는다. 저장소 전체나 큰 파일 전체를 일괄 출력하지 않는다.
- 탐색은 요청과 직접 관련된 디렉터리에서 시작한다. 호출자·공통 모듈·테스트로 이어지는 의존성이 확인될 때만 범위를 넓힌다.
- 이미 읽은 내용은 같은 작업에서 재사용한다. 파일 변경, 누락된 문맥, 검증 필요가 있을 때만 다시 읽는다. 이전 세션의 기억을 현재 파일 내용으로 단정하지 않는다.
- 로그·빌드 결과·생성물·바이너리·잠금 파일은 문제와 관련될 때만 읽는다. 긴 출력은 요약·실패 지점·필요한 줄 범위로 제한하고, 잘린 결과를 전체 결과처럼 판단하지 않는다.
- 외부 문서 조회는 최신 API·버전 동작 등 확인이 필요한 경우에 수행한다. 로컬 코드로 답이 확인되면 동일 내용을 반복 검색하지 않는다.

### 최소 변경과 검증

- 작은 수정은 짧은 실행 계획 후 바로 처리한다. 불필요한 장기 계획, 대안 나열, 전면 재작성, 자동 생성 문서의 반복 갱신을 하지 않는다.
- 독립적인 조회는 가능한 경우 한 번에 묶는다. 앞선 결과에 의존하는 수정과 검증은 순서대로 수행한다.
- 변경 부분만 패치하고 검토한다. 전체 파일을 답변에 재출력하거나 동일 diff를 반복해서 읽지 않는다.
- 테스트는 영향 범위부터 실행하고 성공 시 요약만 확인한다. 실패하면 해당 오류와 관련 로그부터 읽는다.
- 코드·환경·입력이 바뀌지 않은 동일 검증은 반복하지 않는다. 다만 7절의 공통 모듈 검증과 main 반영·릴리스 전 회귀 검증은 유지한다.
- 완료 조건을 충족하면 끝낸다. 요청과 무관한 개선은 필요한 경우 후속 항목 한 줄로 남기고 구현 범위를 늘리지 않는다.
- 여러 에이전트에 동일 문맥을 중복 전달하지 않는다. 병렬 에이전트는 사용자가 요청한 경우에만 역할과 읽을 범위를 좁혀 사용한다.

### 짧게 소통하고 기록하기

- 진행 보고는 중요한 발견·차단 요인·방향 변경 중심으로 짧게 한다. 명령 실행마다 보고하거나 같은 계획을 반복하지 않는다.
- 완료 보고는 8절의 필수 정보를 포함하되 기본 5~8줄을 목표로 한다. 큰 정책 변경이나 검증 실패는 판단에 필요한 만큼 추가한다.
- 사용자가 요청하지 않으면 전체 코드·장문 설명·여러 대안을 붙이지 않는다. 파일 경로와 변경 요약을 제공한다.
- 변경 이력에는 결과와 이유를 기록하고 대화·명령 로그를 복사하지 않는다. 정책의 상세 내용은 `docs/POLICY.md`에 한 번만 정의하고 다른 문서에서는 참조한다.
- 긴 작업의 인계가 필요한 경우 목표, 현재 브랜치, 변경 파일, 확정 결정, 검증 결과, 다음 행동만 짧게 남긴다. 동일 작업을 처음부터 다시 분석하지 않는다.
